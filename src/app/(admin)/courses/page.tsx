import CourseManager from "@/components/modules/course-manager";

export default function CoursesPage() {
  return <CourseManager routeCourseId={null} routeChapterId={null} routeSectionId={null} />;
}
